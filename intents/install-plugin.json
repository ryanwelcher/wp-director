{
  "id": "install-plugin",
  "description": "Install a WordPress plugin from the plugin directory by slug. Does NOT activate the plugin — pair with the activate-plugin intent when the user wants it activated.",
  "examples": [
    "install Jetpack",
    "install the Jetpack plugin",
    "add Jetpack to this site",
    "install hello dolly"
  ],
  "slots": [
    { "name": "slug", "type": "string" }
  ],
  "label": "Install the {{slug}} plugin",
  "actions": [
    { "action": "wpInstallPlugin", "slug": "{{slug}}" }
  ]
}
